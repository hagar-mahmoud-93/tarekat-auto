# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: nafithLogin/login.spec.ts >> Nafath login >> logs in with valid Nafath credentials @regression @nafith-login
- Location: tests/nafithLogin/login.spec.ts:5:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.fill: Target page, context or browser has been closed
```

# Test source

```ts
  1  | import { BasePage } from './base.page';
  2  | import { LoginLocators } from '../locators/login.locators';
  3  | 
  4  | export class LoginPage extends BasePage {
  5  |   private readonly locators = new LoginLocators(this.page);
  6  | 
  7  |   async goto() {
  8  |     await this.page.goto('/login');
  9  |   }
  10 | 
  11 |   async loginWithNafath(username: string, password: string) {
  12 |     await this.locators.nafathLoginLink().click();
> 13 |     await this.locators.usernameInput().fill(username);
     |                                         ^ Error: locator.fill: Target page, context or browser has been closed
  14 |     await this.locators.passwordInput().fill(password);
  15 |     await this.locators.submitButton().click();
  16 |   }
  17 | }
  18 | 
```